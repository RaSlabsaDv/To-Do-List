services:
  postgres:
    image: postgres:latest
    container_name: todo-postgres
    environment:
      POSTGRES_DB: todo
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
    ports:
      - "5432:5432"